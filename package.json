{
  "name": "gateway-auth-app",
  "version": "0.0.0",
  "description": "Serves the auth route that checks for a session cookie and prompts for login",
  "main": "dist/lib/server.js",
  "scripts": {
    "start": "./bin/start",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "./build.sh bower"
  },
  "author": "matt styles @personalurban",
  "license": "ISC",
  "dependencies": {
    "co-body": "0.0.2",
    "co-views": "^0.2.0",
    "hogan.js": "^3.0.2",
    "koa": "^0.8.1",
    "koa-logger": "^1.2.1",
    "koa-route": "^1.1.4",
    "koa-static": "^1.4.5"
  }
}
